{% if section.settings.show_announcement %}
 {% render 'model.announcement-bar.builder' %}
{% endif %}

{% schema %}
  {
    "name": "Announcement bar",
    "settings": [
    {
      "type": "checkbox",
      "id": "show_announcement",
      "label": "Show announcement",
      "default": true
    },
    {
  "type": "text",
  "id": "announcement_text",
  "label": "Heading",
  "default": "Free domestic shipping on all orders"
}
  ],
  "presets": [
    {
      "name": "Announcement Bar",
      "category": "Custom",
      "settings": {
        "show_announcement": true
      }
    }
  ]
  }
{% endschema %}